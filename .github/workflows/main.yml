# AI News Cross-Review "The Jury" - 自動生成ワークフロー
# 月・水・金の朝9時（JST）に自動実行

name: Generate & Deploy AI News Review

on:
  schedule:
    - cron: '0 0 * * 1,3,5'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pillow

      - name: Generate Article
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          python generate_article.py

      - name: Commit and Push new article
        run: |
          git config --global user.name 'The Jury Bot'
          git config --global user.email 'the-jury-bot@users.noreply.github.com'
          git add .
          git diff --staged --quiet || git commit -m "[Bot] 🤖 新記事を自動生成 $(date +'%Y-%m-%d')"
          git push
